/**
 * Используется для провайдинга параметров для запросов к CRM в продовой версии модуля
 * (RPC вызовов)
 */
export const TOKEN_CRM_RPC_REST = Symbol('TOKEN_CRM_RPC_REST');

/**
 * Используется для провайдинга проксей в моковой версии модуля
 */
export const TOKEN_PROXIES_MOCK = Symbol('TOKEN_PROXIES');
